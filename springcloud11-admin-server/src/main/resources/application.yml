spring:
  application:
    name: admin-server
server:
  port: 9301
eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8001/eureka/